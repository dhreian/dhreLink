#pragma once

// dhreLink does not use OBS runtime path discovery. This header supplies the
// generated include expected by the public libobs headers when building the
// standalone module against a matching OBS source tag.
